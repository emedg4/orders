version: '3.8'
services:
  mongodb:
    image: mongo:latest
    environment:
      - MONGODB_DATABASE= "orders"
    volumes:
      - ./mongo:/data/db
    networks:
      - poc
    ports:
      - 27017:27017

networks:
  poc:  